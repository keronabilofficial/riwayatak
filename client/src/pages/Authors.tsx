import PublicLayout from "@/components/PublicLayout";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Search, UserRound } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

export default function Authors() { const [query, setQuery] = useState(""); const { data, isLoading } = trpc.catalog.listAuthors.useQuery({ query: query || undefined, limit: 48 }); return <PublicLayout><section className="container py-14 md:py-20"><p className="text-xs font-bold tracking-[.18em] text-[#af7c42]">أصوات تكتب</p><h1 className="mt-2 font-serif text-5xl">المؤلفون</h1><p className="mt-4 max-w-xl leading-8 text-[#667085]">تعرّف إلى العقول والخيالات التي تقف خلف كل عمل في المكتبة.</p><div className="mt-8 flex max-w-md items-center rounded-2xl border border-[#1d2940]/15 bg-white px-4"><Search className="h-5 w-5 text-[#af7c42]"/><Input value={query} onChange={event=>setQuery(event.target.value)} placeholder="ابحث باسم المؤلف..." className="h-14 border-0 bg-transparent shadow-none focus-visible:ring-0" /></div></section><section className="container pb-24">{isLoading ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-36 animate-pulse rounded-2xl bg-[#e9dfca]"/>)}</div> : data?.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data.map(author=><Link key={author.id} href={`/authors/${author.slug}`} className="group flex min-h-40 gap-5 rounded-2xl border border-[#1d2940]/10 bg-white p-5 transition hover:-translate-y-1 hover:border-[#af7c42]/40 hover:shadow-xl hover:shadow-[#1d2940]/5">{author.imageUrl ? <img src={author.imageUrl} alt={author.name} className="h-20 w-20 rounded-full object-cover"/> : <span className="grid h-20 w-20 shrink-0 place-items-center rounded-full bg-[#e9dfca] text-[#af7c42]"><UserRound/></span>}<span className="self-center"><strong className="font-serif text-2xl">{author.name}</strong><span className="mt-2 line-clamp-2 block text-sm leading-6 text-[#667085]">{author.shortBio || "صفحة المؤلف وأعماله المنشورة."}</span></span></Link>)}</div> : <div className="rounded-2xl border border-dashed border-[#af7c42]/40 py-16 text-center"><h2 className="font-serif text-2xl">لا توجد ملفات مؤلفين منشورة بعد</h2><p className="mt-2 text-sm text-[#667085]">ستظهر هنا ملفات المؤلفين عند إضافتها من لوحة الإدارة.</p></div>}</section></PublicLayout>; }
