import PublicLayout from "@/components/PublicLayout";
import { trpc } from "@/lib/trpc";
import { ArrowLeft } from "lucide-react";
import { Link } from "wouter";

export default function Categories() { const {data, isLoading} = trpc.catalog.categories.useQuery(); return <PublicLayout><section className="container py-14 md:py-20"><p className="text-xs font-bold tracking-[.18em] text-[#af7c42]">خريطة الحكايات</p><h1 className="mt-2 font-serif text-5xl">التصنيفات</h1><p className="mt-4 max-w-xl leading-8 text-[#667085]">مسارات مختلفة للوصول إلى الرواية التي تناسب مزاجك اليوم.</p></section><section className="container pb-24">{isLoading ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-40 animate-pulse rounded-2xl bg-[#e9dfca]"/>)}</div> : data?.length ? <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data.map((category,index)=><Link href={`/categories/${category.slug}`} key={category.id} className="group flex h-44 items-end justify-between rounded-2xl border border-[#1d2940]/10 bg-[linear-gradient(135deg,#fbf8f2,#e9dfca)] p-6 transition hover:-translate-y-1 hover:border-[#af7c42]/40"><span><span className="mb-3 block text-xs font-bold text-[#af7c42]">{String(index+1).padStart(2,"0")}</span><strong className="font-serif text-3xl">{category.name}</strong><span className="mt-2 block text-sm text-[#667085]">{category.description || "روايات منتقاة بعناية"}</span></span><ArrowLeft className="h-5 w-5 text-[#af7c42]"/></Link>)}</div> : <div className="rounded-2xl border border-dashed border-[#af7c42]/40 py-16 text-center text-sm text-[#667085]">أضف التصنيفات من لوحة الإدارة لتظهر هنا.</div>}</section></PublicLayout>; }
